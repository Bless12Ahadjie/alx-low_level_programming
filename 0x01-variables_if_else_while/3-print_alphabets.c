#include <stdio.h>
#include <stdlib.h>
/**
 * main - print both lowe and uppercase alphabets
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(a);
	}
	for (ch = 'A'; ch <= 'Z'; ch++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}
